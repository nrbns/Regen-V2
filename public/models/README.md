# Avatar 3D model (optional)

Place `avatar.glb` here to enable the **3D ready** badge in the browser shell.

Install `@react-three/fiber` and `@react-three/drei` to render the model in `AdaptiveAvatar.tsx`.
